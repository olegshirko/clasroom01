# 🐍 Python OOP: Month 1

Добро пожаловать на курс по основам ООП! В этом репозитории собраны задачи на первый месяц обучения.

## 🚀 Как работать с репозиторием

1. **Клонируйте репозиторий**:
   `git clone <ссылка_на_ваш_репозиторий>`
2. **Установите зависимости**:
   `pip install -r requirements.txt`
3. **Решайте задачи**:
   Все задачи находятся в папках `lab0X/tasks/`. Вам нужно реализовать методы в классах, где написано `pass`.
4. **Проверяйте себя**:
   Запустите тесты командой: `pytest lab01_intro_classes/tests/test_book.py` или `pytest pytest lab01_intro_classes` если решили все задачи из lab01.

## ⚖️ Правила игры
- **Не меняйте файлы в папках `tests/`**. Если вы их измените, CI тесты на GitHub могут не сработать.
- **Минимум 3 коммита в неделю**. Важен и процесс, результатом полученный в последний день не удержится в памяти.
- **Зеленая галочка в GitHub**. Ваша задача считается выполненной, когда все тесты проходят успешно (статус Actions: Success).
- **Ты можешь пользоваться документацией**, но логику классов пиши сам. Это твоя база для будущей карьеры.
## 🛠 Пример решения (Lab 01: Book)
В задаче `book.py` вам нужно создать метод `__init__`, с параметрами `title`, `author` и присвоить их экземпляру класса.
```python
class Book:
    """Задача: book"""
    def __init__(self, title: str, author: str):
        self.title = title
        self.author = author
```

## 📥 Работа с Git (Шпаргалка)
Чтобы твоя работа была засчитана, её нужно отправить на GitHub. Используй этот цикл команд:

1. Сохрани изменения (Commit):

```Bash
git add .
git commit -m "Выполнил задачу Book и Smartphone"
```

2. Отправь на проверку (Push):

```Bash
git push origin main
```
Как понять, что всё ок? Зайди во вкладку Actions своего репозитория на GitHub. Если там горит ✅ — всё отлично. Если ❌ — нажми на неё, чтобы увидеть, какой тест не прошел.